extends layout

block content

	.container.mt-5
		include shared/link-card.pug

		hr.mt-4

		h6 Are you sure you want to delete this link?

		form(method="post")
			button.btn.btn-danger.mr-3(type="submit") Yes, delete
		
			a.btn.btn-primary(href=`/link/${link._id}`) No, don't delete