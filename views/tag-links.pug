extends layout

block content
	h1 #{links.length.toLocaleString()} Link
		| #{links.length != 1 ? "s" : ""} 
		| tagged with 
		span.badge.badge-primary
			i.fas.fa-tag.mr-2
			| #{tag}
	
	include ./shared/link-list.pug