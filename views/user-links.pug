extends layout

block headBlock
	title My Links

block content
	+linkListPageHeader(linkCount, limit, offset, sort, paginationBaseUrl)
		h1 #{linkCount.toLocaleString()} Link
			| #{linkCount != 1 ? "s" : ""}

	hr

	include ./shared/link-list.pug