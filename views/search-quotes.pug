extends layout

block content
	h1 #{quotes.length.toLocaleString()} Quote
		| #{quotes.length != 1 ? "s" : ""}  for 
		.badge.badge-primary #{query}

	.grid
		each quote, quoteIndex in quotes
			.grid-item.col-xl-4.col-sm-6
				include shared/quote-card.pug