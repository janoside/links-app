extends layout

block headBlock
	title My Items

block content
	+itemListPageHeader(itemCount, limit, offset, sort, paginationBaseUrl)
		+pageTitle(`${itemCount.toLocaleString()} Item${itemCount != 1 ? "s" : ""}`)
		

	include ./shared/item-list.pug